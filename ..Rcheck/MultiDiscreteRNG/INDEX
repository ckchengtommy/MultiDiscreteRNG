BinToB                  This function implements step 5 of the
                        algorithm in the paper. It converts the
                        multivariate binary data back to the original
                        binomial scale
BinToGPD                This function implements Step 5 of the
                        algorithm. It converts the multivariate binary
                        data back to the original GPD scale
BinToNB                 This function implements Step 5 of the
                        algorithm. It converts the multivariate binary
                        data back to the original GPD scale
GetGpoisPMF             Get probability mass function of Generalized
                        Poisson distribution
QuantileGpois           This function computes the quantile of
                        Generalized Poisson
calc.bin.prob.B         This function implements Step 1 of the
                        algorithm. It collapses the discrete outcome to
                        binary ones for each variable.
calc.bin.prob.GPD       This function implements Step 1 of the
                        algorithm. It collapses the discrete outcome to
                        binary ones for each variable.
calc.bin.prob.NB        This function implements Step 1 of the
                        algorithm. It collapses the discrete outcome to
                        binary ones for each variable.
discrete_cont           Check positive definiteness of the intermediate
                        matrix
genB                    This function generates multivariate Binomial
                        data
genGPD                  This function generates multivariate GPD data
genNB                   This function generates multivariate NB data
generate.binaryVar      Generate multivariate Binary data using the
                        Emrich and Piedmonte (1991) approach Approach
simBinaryCorr.B         This function implements Step 2 of the
                        algorithm It calculates the intermediate binary
                        correlations.
simBinaryCorr.GPD       This function implements Step 2 of the
                        algorithm It calculates the intermediate binary
                        correlations.
simBinaryCorr.NB        This function implements Step 2 of the
                        algorithm It calculates the intermediate binary
                        correlations.
validation.Bparameters
                        Validate if the input Binomial parameters are
                        within feasible range
validation.GPDparameters
                        Validate if the input GPD parameters are within
                        feasible range
validation.NBparameters
                        Validate if the input NB parameters are within
                        feasible range
